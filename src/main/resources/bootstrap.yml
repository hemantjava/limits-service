spring:
  application:
    name: limits-service
  cloud:
    config:
      name: limits-service
      uri:
        - http://localhost:8888
      label: master
  profiles:
    active: dev
server:
  port: 9090

